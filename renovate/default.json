{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "BX Team base Renovate policy. Every repository extends this through one of the language presets, never directly",
  "extends": ["config:recommended", ":disableDependencyDashboard"],
  "labels": ["🔄 dependencies"],
  "baseBranchPatterns": ["master"],
  "commitMessagePrefix": "chore(deps): ",
  "prConcurrentLimit": 5,
  "prHourlyLimit": 0,
  "packageRules": [
    {
      "description": "Patch bumps are noise once CI is green — let them merge themselves.",
      "matchUpdateTypes": ["patch"],
      "automerge": true
    },
    {
      "description": "One PR for all action bumps instead of one per action.",
      "matchManagers": ["github-actions"],
      "groupName": "github actions"
    },
    {
      "description": "flake.lock inputs move together; a split PR leaves the lock inconsistent.",
      "matchManagers": ["nix"],
      "groupName": "nixpkgs"
    }
  ]
}
